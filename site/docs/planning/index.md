# Product Requirements Document

**Contactless Order System** · Version 1.0 · March 2026  
Status: **Draft — For Client Review**

---

## 1. Executive Summary

The **Contactless Order System** is a SaaS platform that enables restaurants to offer a fully digital, app-free ordering experience. Guests scan a QR code at their table, browse a live digital menu, and place orders directly from their personal device. Orders flow in real time to kitchen staff, who update statuses visible to both guests and front-of-house staff. Restaurant owners manage menus, tables, and reporting through a dedicated dashboard.

The platform is designed for rapid onboarding of multiple restaurants under a single multi-tenant architecture, with strong role separation and real-time communication as core design principles.

---

## 2. Problem Statement

Traditional restaurant ordering suffers from:

- **Staff bottlenecks** — Guests wait for staff to take orders manually, causing delays during peak hours.
- **Error-prone hand-off** — Verbal or handwritten orders are misheard or missed.
- **No guest visibility** — Guests have no insight into order status; they must flag down staff for updates.
- **High printing costs** — Paper menus must be reprinted for every menu change.
- **COVID-era hygiene** — Physical menus and shared surfaces remain a contact risk concern for health-conscious diners.

---

## 3. Product Goals

| # | Goal | Success Metric |
|---|------|----------------|
| G1 | Reduce average time-to-order | < 3 minutes from QR scan to confirmed order |
| G2 | Eliminate order transcription errors | 0 staff-relay orders in normal flow |
| G3 | Guest satisfaction | ≥ 4.2 / 5 post-meal rating |
| G4 | Restaurant onboarding speed | New restaurant live within 30 minutes |
| G5 | Platform uptime | 99.5% monthly uptime SLA |
| G6 | Real-time order latency | Order state updates delivered < 1 second P95 |

---

## 4. Scope

### In Scope (v1.0)

- QR code generation per table
- Guest-facing digital menu browse & order
- Real-time order status tracking (guest + kitchen + staff)
- Thermal print ticket routing (kitchen printer + bar printer)
- Staff management dashboard
- Restaurant owner menu & table management
- Multi-restaurant platform admin panel
- Supabase-backed auth (JWT) and data persistence
- SignalR WebSocket hub for live state sync
- Mobile-first responsive UI (no native app required)

### Out of Scope (v1.0)

- Payment processing / in-app checkout
- Loyalty programs or promo codes
- Inventory management or stock alerts
- Table reservation / booking system
- Native iOS/Android apps
- Third-party POS integrations (planned v2)
- Multi-language menu content (planned v2)

---

## 5. User Personas & Roles

### 5.1 Guest (Diner)

> A customer seated at a restaurant table with a smartphone.

- Has **no account** — accesses the system by scanning a QR code
- Wants to browse the menu, customise items, and place an order
- Wants visibility on order status without flagging down staff
- May place multiple orders during a single visit

### 5.2 Staff (Waiter / Front-of-House)

> A restaurant employee who manages tables and assists guests.

- Authenticates via **PIN** (no password setup required)
- Can view all active tables and orders
- Can mark orders as served / close a table session
- Can override or cancel guest orders

### 5.3 Restaurant Owner / Manager

> The business owner or manager of a single restaurant.

- Full account with **Google OAuth** sign-in — no password setup required
- Account starts in **Pending Approval** status; must be activated by PlatformAdmin after fee payment is confirmed
- Manages the digital menu (add/edit/archive items, set availability)
- Manages tables (add/remove, generate/refresh QR codes)
- Views order history, revenue reports, and guest feedback
- Manages staff PINs and accounts

### 5.4 Platform Administrator

> The operator of the SaaS platform — has access to all tenants.

- Creates and manages restaurant tenants
- Views cross-tenant metrics and health
- Can suspend or deactivate restaurants
- Reviews and **approves** new restaurant owner registrations after fee payment is confirmed
- Manages platform-wide configuration

---

## 6. Functional Requirements

### F1 — QR Code & Table Management

| ID | Requirement | Priority |
|----|-------------|----------|
| F1.1 | Each table has a unique, scannable QR code generated by the system | Must Have |
| F1.2 | QR codes link to a guest session scoped to the specific table and restaurant | Must Have |
| F1.3 | Restaurant owner can add, rename, and deactivate tables | Must Have |
| F1.4 | Restaurant owner can regenerate a QR code for a table (e.g. to invalidate old codes) | Must Have |
| F1.5 | QR code is downloadable as PNG/PDF for physical printing | Should Have |
| F1.6 | QR codes encode a signed, time-independent token — no expiry by default | Must Have |

### F2 — Guest Menu Browse & Ordering

| ID | Requirement | Priority |
|----|-------------|----------|
| F2.1 | Guest accesses menu by scanning QR code — no app install, no account required | Must Have |
| F2.2 | Menu displays categories, item names, descriptions, prices, and photos | Must Have |
| F2.3 | Guest can add items to a cart and adjust quantities | Must Have |
| F2.4 | Guest can add a free-text note per item (e.g. "no onions") | Must Have |
| F2.5 | Guest reviews the cart and confirms order with a single tap | Must Have |
| F2.6 | Guest receives immediate on-screen confirmation with an order number | Must Have |
| F2.7 | Guest can place additional orders during the same table session | Must Have |
| F2.8 | Menu items marked as unavailable are visually disabled and unorderable | Must Have |
| F2.9 | Menu supports item photos (optional per item) | Should Have |
| F2.10 | Guest can view full order history for their current session | Should Have |

### F3 — Real-Time Order Tracking

| ID | Requirement | Priority |
|----|-------------|----------|
| F3.1 | New orders trigger a print event via SignalR — food items routed to kitchen printer, drink items to bar printer | Must Have |
| F3.2 | Order status auto-advances to "Preparing" when the print ticket is dispatched | Must Have |
| F3.3 | Guest sees live order status updates on their device without page refresh | Must Have |
| F3.4 | Status change triggers an on-screen notification to the guest | Should Have |
| F3.5 | Staff dashboard shows all active orders across all tables, sortable by time | Must Have |
| F3.6 | System alerts if an order has been in "Received" state > 5 minutes (configurable) | Could Have |

### F4 — Thermal Print System

| ID | Requirement | Priority |
|----|-------------|----------|
| F4.1 | Each menu item is tagged as **food** or **drink** to determine print routing | Must Have |
| F4.2 | Food items in an order are automatically printed on the **kitchen thermal printer** | Must Have |
| F4.3 | Drink items in an order are automatically printed on the **bar thermal printer** | Must Have |
| F4.4 | Each printed ticket shows: table number, order number, items, quantities, and item notes | Must Have |
| F4.5 | Kitchen and bar printers operate independently — a drink-only order does not trigger the kitchen printer | Must Have |
| F4.6 | A lightweight **print client agent** runs at the restaurant, listening for SignalR print events | Must Have |
| F4.7 | Print failure (offline printer) surfaces a visual alert in the Staff Dashboard | Should Have |

### F5 — Staff Dashboard

| ID | Requirement | Priority |
|----|-------------|----------|
| F5.1 | Staff can view all tables and their current status (free / occupied / needs attention) | Must Have |
| F5.2 | Staff can close a table session and reset the table after the guest has paid at the restaurant counter | Must Have |
| F5.3 | Staff can view and cancel individual orders | Must Have |
| F5.4 | Staff authenticate via 4-6 digit PIN (no email login required) | Must Have |

### F6 — Restaurant Owner Dashboard

| ID | Requirement | Priority |
|----|-------------|----------|
| F6.1 | Owner can create, edit, and archive menu categories | Must Have |
| F6.2 | Owner can create, edit, and archive menu items (name, description, price, photo, availability) | Must Have |
| F6.3 | Owner can toggle item availability in real time (e.g. "sold out") | Must Have |
| F6.4 | Owner can manage tables and download QR codes | Must Have |
| F6.5 | Owner can create and manage staff PINs | Must Have |
| F6.6 | Owner can view order history with filters (date range, table, status) | Should Have |
| F6.7 | Owner can see basic revenue summary (daily/weekly totals) | Should Have |
| F6.8 | Owner configures restaurant profile (name, logo, address, opening hours) | Should Have |

### F7 — Platform Admin Panel

| ID | Requirement | Priority |
|----|-------------|----------|
| F7.1 | Platform admin can create a new restaurant tenant | Must Have |
| F7.2 | Platform admin can view all tenants and their status | Must Have |
| F7.3 | Platform admin can suspend / reactivate a tenant | Must Have |
| F7.4 | Platform admin can view cross-tenant order volume metrics | Could Have |
| F7.5 | Platform admin reviews pending restaurant owner registrations and approves or rejects them | Must Have |
| F7.6 | Approval activates the restaurant owner account; rejection sends a notification email | Must Have |

### F8 — Authentication & Authorization

| ID | Requirement | Priority |
|----|-------------|----------|
| F8.1 | Restaurant owners sign in via **Google OAuth** (Supabase Auth social login); new accounts start in "Pending Approval" state | Must Have |
| F8.2 | Staff auth via restaurant-scoped PIN (no email required) | Must Have |
| F8.3 | Guest sessions are anonymous and scoped to table + restaurant | Must Have |
| F8.4 | Row-Level Security (RLS) enforced in Supabase — tenants cannot read each other's data | Must Have |
| F8.5 | API validates JWT/session on every request | Must Have |
| F8.6 | Role hierarchy enforced: Platform Admin > Owner > Staff > Guest | Must Have |
| F8.7 | Restaurant owner cannot access any platform features until PlatformAdmin approves the account | Must Have |

---

## 7. Non-Functional Requirements

| Category | Requirement |
|----------|-------------|
| **Performance** | Page load < 2s on 4G mobile; order submission round-trip < 500ms P95 |
| **Scalability** | Stateless API; horizontal scaling via Docker blue-green; supports 50 concurrent restaurants at launch |
| **Availability** | 99.5% monthly uptime; zero-downtime deployments via blue-green switch |
| **Security** | OWASP Top 10 mitigations; all data in transit over HTTPS/WSS; no PII in QR tokens |
| **Accessibility** | WCAG 2.1 AA compliance on guest-facing menu pages |
| **Browser Support** | Latest 2 versions: Chrome, Safari, Firefox, Edge; iOS Safari 15+; Android Chrome 100+ |
| **Data Retention** | Order history retained 24 months; then anonymised for reporting |
| **Audit Logging** | All status changes, auth events, and admin actions logged |

---

## 8. Technical Architecture Summary

| Layer | Technology |
|-------|-----------|
| Frontend (All roles) | Vue 3 + **Vuetify** — single unified SPA deployed on **Vercel** |
| Backend API | .NET 8 Minimal API (ASP.NET Core) — on Civo VM |
| Real-Time Hub | ASP.NET Core SignalR (WebSocket) — co-hosted with API on Civo VM |
| Auth & Database | Supabase (PostgreSQL + GoTrue Auth + Google OAuth) |
| File Storage | Supabase Storage (menu item photos) |
| Infrastructure | Civo Compute VM (backend) + Vercel (frontend CDN) |
| Deployment | Docker Compose blue-green (backend on Civo); Vercel Git integration (frontend) |
| CI/CD | GitHub Actions → SSH deploy to Civo (backend); Vercel auto-deploy on push (frontend) |
| Docs | VitePress (this site) |

---

## 9. Data Model (High-Level)

```
Restaurant (owner_status: pending|active|suspended)
  ├── Tables (table_number, qr_token)
  ├── MenuCategories (name, sort_order)
  │   └── MenuItems (name, description, price, photo_url, is_available, item_type: food|drink)
  ├── Staff (name, pin_hash, role)
  └── Orders
        ├── (table_id, session_id, status, created_at)
        └── OrderItems (menu_item_id, quantity, note, status)
```

---

## 10. User Journey: Happy Path

```
1. Guest sits at table → scans QR code
2. Browser opens menu page (no install, no login)
3. Guest browses categories, adds items, optionally adds notes
4. Guest taps "Place Order" → order submitted
5. Food items auto-print on the kitchen thermal printer; drink items auto-print on the bar thermal printer
6. Order status advances to "Preparing" automatically
7. Guest sees ⟳ "Being prepared" on their device
8. Kitchen / bar staff prepare items from the printed ticket — no screen interaction needed
9. Staff delivers food and drinks → marks "Served" in the Staff Dashboard
10. Guest sees ✓ "Order complete"
11. Guest may place additional orders (repeat from step 3)
12. Guest pays at the restaurant counter → Staff closes the table session
```

---

## 11. Release Milestones

| Milestone | Scope | Target |
|-----------|-------|--------|
| **M0 — Foundation** | Repo, CI/CD, infra, VitePress docs | ✅ Done |
| **M1 — Core API** | Auth, restaurant CRUD, menu CRUD, table + QR generation | Sprint 1–2 |
| **M2 — Guest Flow** | QR scan → menu → cart → order submit → confirmation | Sprint 3–4 |
| **M3 — Real-Time** | SignalR hub, kitchen display, live status updates | Sprint 5–6 |
| **M4 — Dashboards** | Staff dashboard, owner dashboard, basic reporting | Sprint 7–8 |
| **M5 — Platform Admin** | Tenant management, cross-tenant metrics | Sprint 9 |
| **M6 — Hardening** | Security audit, performance tuning, accessibility pass | Sprint 10 |
| **v1.0 Launch** | Production deployment, monitoring, SLA in effect | Sprint 11 |

---

## 12. Open Questions

| # | Question | Owner | Status |
|---|----------|-------|--------|
| OQ1 | Will payment processing be in scope for v1.1? | Client | Open |
| OQ2 | Single currency or multi-currency pricing? | Client | Open |
| OQ3 | Should menu items support variants/options (e.g. size, spice level)? | Product | Open |
| OQ4 | Is a native app wrapper (PWA install prompt) desirable? | Client | Open |
| OQ5 | Guest feedback / rating — collected per order or per visit? | Product | Open |
| OQ6 | Are there compliance requirements (GDPR, PDPA)? | Client | Open |

---

*Document maintained by the development team. Last updated: March 2026.*
